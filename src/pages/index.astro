---
import Layout from "../layouts/Layout.astro";
import NumberTicker from "../components/magicui/number-ticker";
import { AnimatedBeamDemo } from "../components/unidirectional-beam";
import FollowPath from "../components/followpath";
---

<Layout title="Welcome to Astro.">
  <main>
    <div class="flex flex-col gap-y-10 items-center justify-center">
      <NumberTicker value={100} client:visible />
      <AnimatedBeamDemo client:load />
      <!--  <FollowPath client:load /> -->
    </div>
  </main>
</Layout>
